# @arcade/orchestrator

What drives the agents: the assessment pipeline and the live run engine. Sits above `@arcade/agents` and `@arcade/core`; anything in the core that needs to call an agent belongs here instead.
