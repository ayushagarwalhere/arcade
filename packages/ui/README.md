# @arcade/ui

What the marketing site and the ADE share: components (`src/components`), hooks (`src/hooks`), site constants and theme logic (`src/lib`) and the design tokens (`src/styles/theme.css`).

A file belongs here only when both apps import it.
