# @arcade/core

Platform-neutral core: the data model, the run engine, the scanner and rules, and the workspace / GitHub file systems. No DOM or Next.js dependency — the mobile app runs copies of these files (`apps/mobile/scripts/sync-core.mjs`).

Import by file: `import { … } from "@arcade/core/engine"`.
